<div
  block:scope
  ...attributes
>
  {{#if @records}}
    {{#each @records as |record index|}}
      {{#unless index}}
        <Ui::List::Row
          @metaFields={{@metaFields}}
          @record={{record}}
          @onClick={{@onClick}}
          @head={{true}}
        />
      {{/unless}}
      <Ui::List::Row
        @disabledRecords={{@disabledRecords}}
        @metaFields={{@metaFields}}
        @onClick={{@onClick}}
        @record={{record}}
        @selectedRecords={{@selectedRecords}}
        @index={{index}}
      />
    {{/each}}
  {{else}}
    Keine {{t (concat "models.plurals." @modelName)}} für diese Auswahl
  {{/if}}
</div>
