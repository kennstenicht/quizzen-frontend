<LinkTo
  @route="profile.user"
  @model={{@user}}
  block:scope
  block:size={{@size}}
  block:color={{@color}}
  ...attributes
>
  {{#if @user.avatar}}
    <img src="" alt="{{@user.nickname}} Avatar" block:class="image">
  {{else}}
    {{@user.initials}}
  {{/if}}
</LinkTo>
